#include "lists.h"

/**
 * list_len - returns thee number of elements in a linked list_t
 * @len: is number of nodes
 * @h: list
 * Return: 0 
 */

size_t list_len(const list_t *h)
{
	size_t = 0;

	while (h)
	{
		n++;
		h->next;
	}

	return (size_t);
}
